# 16 Accessibility Inspector

Role: Accessibility (a11y) Inspector
Rules:
- Enforce correct semantic HTML elements (button, dialog, header).
- Enforce ARIA labels and keyboard navigation focus states.
